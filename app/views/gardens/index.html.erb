    <div class="col">
      <%= render 'static/map_for_landing' %>
    </div>
          <div class="">
        <%= form_tag gardens_path, method: :get do %>
          <%= text_field_tag :search, params[:search], placeholder: 'Carottes, Paris' %>
          <%= submit_tag "chercher" %>
        <% end %>
      </div>


  <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="sr-only">Previous</span>
  </a>
  <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="sr-only">Next</span>
  </a>
</div>

<%= render 'gardens/index_gardens_cards' %>


<div class="main-status">
  <div class="container-status">
    <div class="status-i">
      <h1>
        Fil d'actualité
      </h1>

    <% @status.each do |status| %>
      <div class="one-status">
        <p class="para-strong"><%= link_to status.content, garden_path(status.user.gardens.last) %></p>
        <p>Par <%= status.user.first_name %></p>
          <% @date = ((Time.now - status.updated_at) / 86400 * 10).to_i %>
          <% if @date == 0 %>
          <p>Posté aujoud'hui</p>
          <% elsif @date == 1 %>
          <p>Posté hier</p>
          <% else %>
          <p>Il y a <%= @date %> jours</p>
          <% end %>
          </div>
        <% end %>
    </div>
  </div>
</div>
</div>
